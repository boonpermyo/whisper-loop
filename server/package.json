{
  "name": "@whisper-loop/server",
  "author": "Boonperm Yo by sdsarun",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "rimraf dist && tsc --build tsconfig.build.json",
    "start": "node dist/main.js",
    "dev": "tsx watch src/main.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "type": "module",
  "dependencies": {
    "@whisper-loop/core": "^1.0.0",
    "fastify": "^5.6.1"
  },
  "devDependencies": {
    "@whisper-loop/typescript-config": "^1.0.0",
    "tsx": "^4.20.6"
  }
}
